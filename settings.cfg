# HeroesAI configuration file.
# Lines starting with '#' or ';' are comments and are ignored.
# Edit a value then restart the application -- changes are not hot-reloaded.

# Window
window_width    = 1920
window_height   = 1080
window_title    = HeroesAI
framerate_limit = 60
